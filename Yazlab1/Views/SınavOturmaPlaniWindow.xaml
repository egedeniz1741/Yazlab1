<Window x:Class="Yazlab1.Views.SinavOturmaPlaniWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Yazlab1.Views"
        xmlns:vm="clr-namespace:Yazlab1.ViewModel"
        xmlns:models="clr-namespace:Yazlab1.Model"
        mc:Ignorable="d"
        Title="Sınav Oturma Planları" Height="700" Width="1100" WindowStartupLocation="CenterScreen">

    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="400"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <GroupBox Grid.Column="0" Header="Oluşturulan Sınavlar" Margin="0,0,10,0" FontWeight="Bold">
            <DataGrid ItemsSource="{Binding SinavListesi}"
                      SelectedItem="{Binding SecilenSinav, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      CanUserResizeColumns="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Ders" Binding="{Binding SinavDetay.Ders.DersAdi}" Width="*"/>
                    <DataGridTextColumn Header="Tarih" Binding="{Binding Tarih, StringFormat='dd.MM.yyyy'}" Width="Auto"/>
                    <DataGridTextColumn Header="Saat" Binding="{Binding BaslangicSaati, StringFormat='hh\\:mm'}" Width="Auto"/>
                    <DataGridTextColumn Header="Derslikler" Binding="{Binding DerslikKodlariString}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <GroupBox Grid.Row="0" Header="Seçilen Sınavın Oturma Planı Görseli" FontWeight="Bold">
                <TabControl ItemsSource="{Binding GorselOturmaPlaniListesi}">

                    <TabControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:DerslikOturmaPlani}">
                            <TextBlock Text="{Binding Derslik.DerslikAdi}" FontWeight="Bold"/>
                        </DataTemplate>
                    </TabControl.ItemTemplate>

                    <TabControl.ContentTemplate>
                        <DataTemplate DataType="{x:Type vm:DerslikOturmaPlani}">
                            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding Plan}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <UniformGrid Columns="{Binding Derslik.EnineSiraSayisi}"
                                                         Rows="{Binding Derslik.BoyunaSiraSayisi}"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="{x:Type vm:OturmaPlaniOgrenciDetay}">
                                            <Border BorderBrush="Gray" BorderThickness="1" Margin="2" MinHeight="40" Background="#FFF0F8FF">
                                                <TextBlock Text="{Binding DisplayText}" 
                                                           TextWrapping="Wrap" 
                                                           TextAlignment="Center" 
                                                           VerticalAlignment="Center"
                                                           FontSize="9"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </DataTemplate>
                    </TabControl.ContentTemplate>
                </TabControl>
            </GroupBox>

            <Button Grid.Row="1" 
                    Content="Seçili Sınavın Öğrenci Yerleşim Listesini PDF Olarak Aktar" 
                    Command="{Binding PdfAktarCommand}" 
                    Height="35" FontWeight="Bold" Background="#B71C1C" Foreground="White" Margin="0,15,0,0"/>
        </Grid>
    </Grid>
</Window>